#!/bin/bash
#xinput enable 'SynPS/2 Synaptics TouchPad'
en=$(xinput list-props 'SynPS/2 Synaptics TouchPad' | grep Enabled | cut -d: -f2)
if [ $en -eq 1 ];
then
        xinput disable 'SynPS/2 Synaptics TouchPad'
else
        xinput enable 'SynPS/2 Synaptics TouchPad'
fi


